# Official Article Plugin

This directory is the source artifact for the optional `official.article` Module.
It is included in the Peanut Admin source repository but is not enabled in a
generated application's empty `plugins.lock`. Install it explicitly, then enable
the Module for each Tenant.
