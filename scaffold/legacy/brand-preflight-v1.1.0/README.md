# Retired brand-only preflight fixture

This directory preserves the historical two-file `v1.1.0` dry-run evidence that existed before a complete scaffold release contract. It is not a release, does not contain a complete managed inventory, and must never be accepted by `apply`, `verify`, or `recover`.

The executable `v1.1.0` identity is generated under `scaffold/releases/v1.1.0/` from its manifest's immutable `source_commit` and `source_tree`.
