# 开发环境留空（由 scripts/local-client-dev 按登记的 PHP_PORT 配置 /api 代理）
# 生产环境填写后端地址，如 https://api.example.com
VITE_API_BASE_URL=

# 同一 Release 的部署模式；缺失或非法值一律按 standalone 关闭租户选择与平台入口。
VITE_DEPLOYMENT_MODE=standalone
