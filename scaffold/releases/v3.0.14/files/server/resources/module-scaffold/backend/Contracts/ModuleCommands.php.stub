<?php
declare(strict_types=1);

namespace ${PHP_NAMESPACE}\Contracts;

use PeanutAdmin\Kernel\Auth\TenantContext;

/**
 * Public synchronous command boundary for ${MODULE_KEY}.
 *
 * Add stable business methods here. Keep TenantContext as an explicit input,
 * return DTOs or scalar business values, and never expose PDO, ORM models, or
 * Module-owned table names through this contract.
 */
interface ${MODULE}Commands
{
    // Example: public function execute(TenantContext $context, CommandInput $input): CommandResult;
}
