; Transport limits must not reject a request before UploadService applies its
; per-type limits (image 10 MB, file 50 MB, video 200 MB).
upload_max_filesize = 200M
post_max_size = 210M
max_file_uploads = 20
