# Intentionally empty. Container runtimes receive configuration from Compose.
# Mounting this file prevents an untracked host server/.env from overriding
# the isolated Docker database and other container-only settings.
