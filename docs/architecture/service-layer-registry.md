# Peanut Admin 服务层登记

机器可读事实源：[resources/service-registry.json](../../resources/service-registry.json)。执行状态和业务回报见 [service-execution-status.md](service-execution-status.md)。

## 结论

当前登记 14 个内部复用服务。这里的“服务”是同一应用进程内的稳定合同和唯一 Runtime，不是拆成 14 个微服务。

截至当前 `dev=d1d9e474…`：

- 13 个服务已经有固定提交、最低验证证据、明确数据 owner，并合入 `dev`；
- 统一存储已有候选 PR #232，但仍等待真实 COS 生命周期和破坏性迁移的备份恢复证据；
- 没有新的产品决策阻塞；真实短信、真实支付等属于后置业务验收，不影响本轮服务治理收口。

## 服务分组

### 始终可用的通用服务

Tenant 设置、管理身份与 RBAC、字典与参考码、外部渠道绑定、Plugin/Module 治理、幂等命令、统一审计。

这些服务负责跨模块共享的规则、授权、事务边界和数据访问合同。业务代码应调用它们的 Commands/Queries/Runtime，不应直接写其他模块的私有表。

### 可启停的业务服务

通知与验证码、会员账户与 CRM、支付充值退款、OAuth 与微信渠道、Tenant 任务与异步执行、导入导出。

这些能力由官方 Module 承载，可以按租户启用或停用；Module 仍拥有自己的业务表和业务规则，通用服务不替代其领域逻辑。

### 混合边界

统一存储与文件媒体同时提供始终存在的 Driver/Account/Space 装配和可按租户使用的文件能力。它是本轮唯一尚未完成最终验收的服务。

## 执行规则

1. 服务目标以登记文件为准，专用对话只是执行者；idle 不是完成证据。
2. `complete` 必须同时具备固定 commit/tree、最低验证、数据 owner 和 `dev` 合入事实。
3. 未提交候选只能是待验收，不能作为下游稳定依赖。
4. 共享写表只能有一个 owner；跨 Module 只能通过公开合同访问。
5. 遇到无副作用、可逆且不改变业务语义的问题，主控自动修正并继续；涉及破坏性迁移、身份/RBAC、Schema、资金、真实外部资源或未冻结存储合同时保留停止线。
6. CodeGraph 是按 worktree 建立的可再生索引；需要调用关系或影响范围分析时，先运行 `scripts/project-codegraph ensure`。

## 本轮收口顺序

1. 完成统一存储 COS 上传、下载、删除和删除后读取失败验证。
2. 完成存储迁移前备份、恢复和迁移后账本核对。
3. 由主控验收 PR #232 并合入 `dev`。
4. 对会员头像、OAuth 文件和导入导出对象交付运行一次受影响路径回归。
5. 固化最终 14 服务快照；真实短信、真实支付和其他 Provider 联调另列后置验收，不回改本轮服务治理结论。
