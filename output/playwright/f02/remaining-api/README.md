# F02 剩余退款记录 API 验收

## Peanut

- `export=1`：通过，返回 `40000 / 该列表不支持导出`。
- `sn=F0226080101RI` + start-only `2026-08-01 13:00:00`：通过，命中 1 条 RI。
- `sn=F0226080101RI` + end-only `2026-08-01 17:00:00`：通过，命中 1 条 RI。

## LikeAdmin

- 角色 3 的菜单关系、五个登记权限和真实 `adminapi` 权限缓存恢复后，使用临时管理员真实登录成功。
- `export=1`：通过，返回 `code=0 / 该列表不支持导出`。
- `sn=F0226080101RI` + start-only `2026-08-01 13:00:00`：通过，命中 1 条 RI，`extend={total:1,ing:1,success:0,error:0}`。
- `sn=F0226080101RI` + end-only `2026-08-01 17:00:00`：通过，命中 1 条 RI，`extend={total:1,ing:1,success:0,error:0}`。
- 最终业务层证据见 `likeadmin-results.json`；旧的鉴权失败文件仅保留为排查历史，不作为业务验收结论。

## 双端结论

- 三项业务语义一致并通过：不支持导出的规则和提示一致，起始时间单边筛选、结束时间单边筛选均命中同一业务夹具，数量和状态汇总一致。
- LikeAdmin 使用 `0/1` 响应码，Peanut 使用既有 `40000/20000` 响应码；这是两端统一响应封装约定的差异，不改变本项业务规则与结果。
