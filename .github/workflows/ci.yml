name: CI

on:
  pull_request:
    branches:
      - dev
      - main
  push:
    branches:
      - dev
      - main

permissions:
  contents: read

jobs:
  php:
    name: PHP 8.3
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: mt02_root
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -pmt02_root"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      MYSQL_ROOT_PASSWORD: mt02_root
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          tools: composer:v2
          coverage: none

      - name: Cache Composer downloads
        uses: actions/cache@v4
        with:
          path: ~/.composer/cache/files
          key: ${{ runner.os }}-php-8.3-composer-${{ hashFiles('server/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-php-8.3-composer-

      - name: Validate Composer manifest
        working-directory: server
        run: composer validate --strict

      - name: Install Composer dependencies
        working-directory: server
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Check PHP syntax
        run: find server/app server/config server/database server/route -type f -name '*.php' -print0 | xargs -0 -n1 php -l

      - name: Test website settings contract
        working-directory: server
        run: php tests/Productization/WebsiteConfigServiceTest.php

      - name: Test scaffold brand bootstrap
        working-directory: server
        run: php tests/Productization/BrandScaffoldTest.php

      - name: Test secure installer bootstrap
        working-directory: server
        run: php tests/Productization/InstallerBootstrapTest.php

      - name: Test admin permission Host
        working-directory: server
        run: php tests/Productization/AdminPermissionHostTest.php

      - name: Test reference codes Host ownership
        working-directory: server
        run: php tests/Productization/ReferenceCodesHostTest.php

      - name: Test file media Host ownership
        working-directory: server
        run: php tests/Productization/FileMediaHostTest.php

      - name: Test operations Host ownership
        working-directory: server
        run: php tests/Productization/OpsHostTest.php

      - name: Test member finance Host ownership
        working-directory: server
        run: php tests/Productization/MemberFinanceHostTest.php

      - name: Test content decoration Host ownership
        working-directory: server
        run: php tests/Productization/ContentDecorationHostTest.php

      - name: Test MT02 Article tenant isolation
        working-directory: server
        run: php tests/Multitenancy/ArticleTenantIsolationTest.php

      - name: Test notification Host ownership
        working-directory: server
        run: php tests/Productization/NotificationHostTest.php

      - name: Test MT03 notification tenant isolation
        working-directory: server
        run: php tests/Multitenancy/NoticeTenantIsolationTest.php

      - name: Test payment Host ownership
        working-directory: server
        run: php tests/Productization/PaymentHostTest.php

      - name: Test OAuth and channel Host ownership
        working-directory: server
        run: php tests/Productization/OAuthChannelHostTest.php

      - name: Test MT03 tenant cache and lock isolation
        working-directory: server
        run: php tests/Multitenancy/TenantCacheLockIsolationTest.php

      - name: Test MT03 ThinkPHP tenant cache adapter
        working-directory: server
        run: php tests/Multitenancy/ThinkPhpTenantCacheAdapterTest.php

      - name: Test MT03 file tenant ownership
        working-directory: server
        run: php tests/Multitenancy/FileTenantIsolationTest.php

      - name: Test MT03 operation log tenant attribution
        working-directory: server
        run: php tests/Multitenancy/OperationLogTenantIsolationTest.php

      - name: Test MT03 crontab tenant isolation
        working-directory: server
        run: php tests/Multitenancy/CrontabTenantIsolationTest.php

      - name: Test MT03 hot-search tenant ownership
        working-directory: server
        run: php tests/Multitenancy/HotSearchTenantIsolationTest.php

      - name: Test PM01 platform operator boundary
        working-directory: server
        run: php tests/Multitenancy/PlatformOperatorBoundaryTest.php

  web:
    name: Web
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: web
        env:
          HUSKY: '0'
        run: pnpm install --frozen-lockfile

      - name: Check generated brand files
        run: node scripts/sync-brand-assets.mjs --check

      - name: Build
        working-directory: web
        run: pnpm run build

      - name: Test permission policy
        working-directory: web
        shell: bash
        run: |
          test_output="$(mktemp -d)"
          ./node_modules/.bin/tsc --target ES2020 --module commonjs --moduleResolution node --strict --skipLibCheck \
            --outDir "${test_output}" src/core/permission-policy.ts tests/Productization/permission-policy.test.ts
          node "${test_output}/tests/Productization/permission-policy.test.js"

  pc:
    name: PC
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: pc/package-lock.json

      - name: Install dependencies
        working-directory: pc
        run: npm ci

      - name: Typecheck
        working-directory: pc
        run: npm run typecheck

      - name: Generate
        working-directory: pc
        run: npm run generate

  uniapp:
    name: UniApp
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: uniapp/package-lock.json

      - name: Install dependencies
        working-directory: uniapp
        run: npm ci

      - name: Typecheck
        working-directory: uniapp
        run: npm run type-check

      - name: Build H5
        working-directory: uniapp
        run: npm run build:h5

  docs-site:
    name: Docs site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: docs-site/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: docs-site
        run: pnpm install --frozen-lockfile

      - name: Build
        working-directory: docs-site
        run: pnpm run build
