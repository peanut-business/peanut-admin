# Peanut Admin Agent 执行规则

本文件是 Peanut Admin 仓库的执行规则入口。仓库状态、产品路线和命名约定仍以
`AGENTS.md` 及其引用的权威计划为准。

## 1. 事实和完成证据

- 在判断代码、架构或完成度前，读取相关关键实现；明确区分已确认事实和未经验证的推断。
- 聊天、计划、任务标题、状态转述和“已通知”不是完成证据。代码事实需要对应 diff、完整 commit/tree、测试或运行结果。
- `development-complete`、`qualified`、`release-ready`、可供下游消费和可进入生产是不同状态，不得混用。
- 终态报告实际修改、完整提交、已运行检查、未运行检查、风险和剩余工作。

## 2. 授权、风险和停止线

- 在用户已经批准的范围内主动完成普通、可逆工作，不为常规实现虚构人工 Gate。
- 真实资金或交易、生产数据的破坏性迁移或删除、Git 历史重写、未知外部副作用，以及用户明确保留的人工 Gate，必须在动作前确认。
- 凭据只用于已授权的目标动作，不输出、不记录、不提交。
- 修改前确认仓库、基线、分支和精确写集；禁止覆盖或夹带其他人的修改。

## 3. Gate 最小阻塞闭包

- 失败或等待必须先按影响计算最小阻塞范围，不得默认冻结整个阶段：
  - 安全、Tenant 隔离、授权、数据损坏和核心业务断言失败，阻塞受影响实现及其直接下游；
  - Schema/API 合同和不可变依赖身份失败，阻塞依赖该产物的采用、合入或发布；
  - 格式、文档、依赖元数据和机械 CI 失败，只阻塞当前 PR 收口；
  - 外部 Registry、凭据或长 CI 等待，只阻塞对应外部动作和最终消费者验证。
- “允许并行”不等于 Gate 通过。失败候选留在功能或 stacked-fix 分支；红色 required checks 不得进入 `dev` 或 `main`。
- 与受阻产物没有依赖、文件 owner 不冲突的工作继续推进。通常保持一条关键路径和最多两条真正有价值的独立工作线；没有并行项时说明具体依赖边。
- “审计、准备、研究、即将开始”本身不算推进；至少形成 diff、commit、PR、fixture、可执行合同、命令结果或更新后的恢复指针之一。

## 4. 验证与状态更新

- 每个验收条件只运行一次最低充分验证。manifest 或 lock 变更包含 CI 等价严格校验和精确身份检查。
- 失败按原因去重；只重跑失败组，不重复已经通过的 Gate，也不因局部失败扩大测试套件。
- 测试必须实际命中所声明的分支；不得用较弱场景代替租户隔离、授权、事务或真实数据库证据。
- 状态只在开始、实质变化、异常、超时和完成时更新，并明确仓库、PR 和证据；不重复输出无变化轮询结果。

## 5. Git、并发和工作树

- 同一文件同一时刻只有一个 owner。共享数据库、容器、缓存、服务和其他可变资源也必须有唯一 owner 和项目隔离。
- 长任务和并行写任务使用独立分支与 worktree；临时目录不得成为长期事实源。来源不明或包含未提交修改的工作树不得清理。
- 禁止 `git add .`、`git commit -a`、`git reset --hard` 和盲目覆盖。暂存后检查文件清单并运行 `git diff --cached --check`。
- 密钥、Cookie、缓存、临时日志、测试数据库数据和构建产物不得提交。
- 公共接口和下游采用必须固定完整 commit/tree、版本来源、兼容说明和验证证据，不能只引用分支名或移动 HEAD。

## 6. 技术纪律

- 修改前读取同层级实现，遵循现有技术栈、命名、错误处理和调用模式。
- 不顺手重构、升级依赖或扩大功能；新增依赖必须有明确用例和已接受的依赖决定。
- 数据库、缓存、队列和文件资源使用项目 namespace；一次性测试资源使用唯一 run ID，不得清理其他项目资源。
- 服务地址和凭据通过环境配置，不把个人绝对路径、真实密码或固定开发主机写成业务默认。
- 不以不兼容签名覆盖框架或基类方法；横切行为优先使用框架既定扩展点。
